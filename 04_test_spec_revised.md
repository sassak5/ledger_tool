# 帳簿自動作成ツール テスト仕様
更新日: 2026-02-21

---

## 0. テスト方針

| 方針 | 内容 |
|------|------|
| **3層構成** | ユニット（モジュール単体）→ 統合（ファイル入力→出力）→ 回帰（固定サンプル差分検知） |
| **最優先** | やよいスマート取引取込CSV の整合性を最優先で担保 |
| **インターフェース適合** | 各パーサは `base_parser` の共通インターフェースに準拠するため、インターフェース適合テストを設ける |
| **確定/推奨の区分** | 現段階で仕様が確定しているテストは無印、確定度が低い/将来対応のものは **(推奨)** と表記 |

---

## 1. detector.py — フォーマット検出

### 1.1 正常系

| # | テスト内容 | 入力 | 期待結果 |
|---|----------|------|---------|
| 1 | テンプレA（UFJ系）判定 | UFJヘッダ行を含むCSV | `source_kind="bank_statement"`, `format_id="mufj_table_v1"` |
| 2 | テンプレB（愛知銀行系）判定 | 愛知銀行ヘッダ行を含むCSV | `source_kind="bank_statement"`, `format_id="aichibank_table_v1"` |
| 3 | Amazon決済レポート判定 | Amazon TSVヘッダ行 | `source_kind="amazon_settlement_report"` |
| 4 | 余剰列があっても包含一致で許容 | UFJヘッダ + 追加列「備考2」 | 正常に `mufj_table_v1` と判定 |
| 5 | UTF-8エンコーディング検出 | UTF-8のCSV | `encoding="utf-8"` |
| 6 | cp932エンコーディング検出 | cp932のCSV | `encoding="cp932"` |

### 1.2 異常系

| # | テスト内容 | 入力 | 期待結果 |
|---|----------|------|---------|
| 7 | 必須列欠損 | ヘッダに「日付」がないCSV | `FormatNotMatchedError` |
| 8 | 空ファイル | 0バイトファイル | `FormatNotMatchedError` |
| 9 | 全エンコーディング失敗 | バイナリファイル | `EncodingDetectionError` |
| 10 | 既知テンプレに一致しないヘッダ | 未知の列名セット | `FormatNotMatchedError` |

### 1.3 推奨

| # | テスト内容 | 入力 | 期待結果 |
|---|----------|------|---------|
| 11 | **(推奨)** 表記揺れ許容（全角/半角） | `"お預り金額（円）"` と `"お預り金額(円)"` | 同一エイリアスとして認識 |
| 12 | **(推奨)** ヘッダ行が2行目以降にある場合 | タイトル行 + ヘッダ行の構成 | 正しい行を検出 |

---

## 2. registry.py — パーサレジストリ

### 2.1 正常系

| # | テスト内容 | 入力 | 期待結果 |
|---|----------|------|---------|
| 1 | 登録済みキーでパーサ取得 | `"bank_statement"` | `BankParser` インスタンス |
| 2 | 登録済みキーでパーサ取得 | `"amazon_settlement_report"` | `AmazonParser` インスタンス |

### 2.2 異常系

| # | テスト内容 | 入力 | 期待結果 |
|---|----------|------|---------|
| 3 | 未登録キー | `"unknown_source"` | `UnknownSourceKindError` |
| 4 | 空文字キー | `""` | `UnknownSourceKindError` |

### 2.3 推奨

| # | テスト内容 | 入力 | 期待結果 |
|---|----------|------|---------|
| 5 | **(推奨)** 新パーサを動的登録後に取得可能 | 登録 → 取得 | 新パーサが返る |

---

## 3. base_parser.py — インターフェース適合

| # | テスト内容 | 入力 | 期待結果 |
|---|----------|------|---------|
| 1 | `BankParser` が `parse()` を実装している | — | `hasattr` / ABC適合チェック |
| 2 | `AmazonParser` が `parse()` を実装している | — | `hasattr` / ABC適合チェック |
| 3 | `parse()` の戻り値が `list` であること | 任意の有効入力 | `isinstance(result, list)` |
| 4 | 戻り値の各要素が `RawRecord` 型であること | 任意の有効入力 | 各要素の型チェック |
| 5 | **(推奨)** `CreditCardParser` も適合 | — | 将来実装時に追加 |

---

## 4. parsers/bank.py — 銀行明細パーサ

> **注意（MVP実装）**: 銀行パーサは「CSV行→RawRecord」の生成とヘッダ別名解決に特化し、日付/金額の型変換や入出金方向の判定は `normalizer` 側でテストする。

### 4.1 RawRecord 生成（型変換はしない）

| # | テスト内容 | 入力 | 期待結果 |
|---|----------|------|---------|
| 1 | ヘッダ検出＋列マッピング | UFJヘッダ＋1行 | `header_aliases` に基づき、正規列名（date/summary/...）のキーで `RawRecord.fields` が構成される |
| 2 | 日付・金額は文字列のまま保持 | date="2026/1/5", amount="43,472" | RawRecord では文字列のまま（型変換は normalizer が担当） |
| 3 | 必須列欠損 | ヘッダに date 相当がない | `ValidationError(level="error")`（パーサ段階） |

### 4.2 行スキップ

| # | テスト内容 | 入力 | 期待結果 |
|---|----------|------|---------|
| 4 | 空行 | 全セル空の行 | スキップ（RawRecord に含まれない） |
| 5 | 合計行 | 「合計」を含む行 | スキップ |
| 6 | **(推奨)** 見出し行（小見出し） | ヘッダ以外の見出し行 | スキップ |

> 入出金の最終判定（amount_out/amount_in の片側化、0以上の契約強制）やテンプレ別の細かい差異吸収は normalizer の責務でテストする。

---

## 5. parsers/amazon.py — Amazon決済レポートパーサ

### 5.1 正常系

| # | テスト内容 | 入力 | 期待結果 |
|---|----------|------|---------|
| 1 | TSV読み込み | 有効なAmazon TSV | パースエラーなし |
| 2 | settlement-id 単位のグルーピング | 複数 settlement-id を含むTSV | settlement-id ごとに分離 |
| 3 | バケット集計: 商品代金 | `ItemPrice/Principal` の行 | 商品代金バケットに集計 |
| 4 | バケット集計: 税金（formula） | `Tax` + `ShippingTax` - `TaxDiscount` | 税金バケットの合計が数式通り |
| 5 | normalize=RAW: 符号保持 | 負値を含む行 | 符号がそのまま |
| 6 | normalize=ABS: 絶対値 | 負値を含む行 | 正値に変換 |
| 7 | normalize=NEGATE: 符号反転 | 正値の行 | 負値に変換 |
| 8 | ledger_defaults 付与 | 任意のバケット | デフォルト科目が紐付く |

### 5.2 unmapped / 揺れ耐性

| # | テスト内容 | 入力 | 期待結果 |
|---|----------|------|---------|
| 9 | 未知の amount_description | include に無い description | `unmapped` バケットに集約 |
| 10 | unmapped 警告の内容 | unmapped 行あり | `ValidationError(level="warn")` に件数・合計金額を含む |
| 11 | **(将来)** total-amount 突合一致 | 全バケット合計 == total-amount | 警告なし |
| 12 | **(将来)** total-amount 突合不一致 | 合計に差異あり | `ValidationError(level="warn", field="total_amount")` |

### 5.3 異常系

| # | テスト内容 | 入力 | 期待結果 |
|---|----------|------|---------|
| 13 | 必須列欠損 | amount 列がないTSV | `ValidationError(level="error")` |
| 14 | **(推奨)** settlement-id が空の行 | settlement-id 空白行 | エラーまたはスキップ |

---

## 6. ruleset/loader.py — ルールファイル読込（軽量チェック）

> **注意（MVP実装）**: `ruleset/` 配下には `loader.py` のみが存在し、汎用的なYAMLスキーマ検証ロジックは未実装。ここでは、最低限の必須キー/型チェックに対するテストを想定する。

| # | テスト内容 | 入力 | 期待結果 |
|---|----------|------|---------|
| 1 | 必須キー不足を検出 | ledgerマッピングYAMLから必須キーを削除 | ロード時に例外が送出される |
| 2 | 型不一致の検出 | 本来 dict であるべき箇所に list を指定 | ロード時に例外が送出される |
| 3 | パス不正 | 存在しないYAMLパスを指定 | ファイル不存在エラー |

---

## 7. normalizer.py — 正規化変換

> **注意（MVP実装）**: YAMLの `mapping`/`fields` DSL は未使用であり、`normalizer` 内のヘルパ関数（`_parse_date` / `_parse_int` / `_collapse_spaces` / `normalize_bank` / `normalize_amazon` 等）が直接変換を行う。

| # | テスト内容 | 入力 | 期待結果 |
|---|----------|------|---------|
| 1 | 日付パース（複数フォーマット） | "2026/1/5" / "2026-01-05" / "2026.1.5" | すべて "2026-01-05" に正規化される |
| 2 | 金額パース（カンマ/通貨記号） | "43,472" / "¥12,345" | `43472` / `12345` |
| 3 | 括弧負値→abs | "(500)" | `500`（amount_out/amount_in は 0以上） |
| 4 | row_id 生成 | source_name="mufj", row_number=5 | `row_id="mufj_5"` |
| 5 | 摘要連結 (summary + details) | summary="カード", details="引落し" | `"カード 引落し"` |
| 6 | 摘要: 空白圧縮 | `"カード　 引落し"` (全角+半角空白) | `"カード 引落し"` |
| 7 | extra マッピング | UFJ固有列あり | `extra` dict に格納 |
| 8 | raw 保持 | 元行データ付き RawRecord | `raw` に元データが残る |
| 9 | 必須フィールド欠損 | fields に `date` なし | `ValidationError(level="error")` |
| 10 | 型変換失敗 | amount_out が文字列 `"abc"` | `ValidationError(level="error")` |

---

## 8. validator.py — バリデーション

### 8.1 必須フィールド

| # | テスト内容 | 入力 | 期待結果 |
|---|----------|------|---------|
| 1 | date が正常値 | `date="2026-01-05"` | 通過 |
| 2 | date が null | `date=None` | `ValidationError(level="error", field="date")` |
| 3 | date が空文字 | `date=""` | `ValidationError(level="error", field="date")` |

### 8.2 入出金整合

| # | テスト内容 | 入力 | 期待結果 |
|---|----------|------|---------|
| 4 | 出金のみ正常 | `amount_out=10000, amount_in=0` | 通過 |
| 5 | 入金のみ正常 | `amount_out=0, amount_in=5000` | 通過 |
| 6 | 両方正値 | `amount_out=10000, amount_in=5000` | `warn` |
| 7 | 両方ゼロ | `amount_out=0, amount_in=0` | `warn` |

### 8.3 残高照合

| # | テスト内容 | 入力 | 期待結果 |
|---|----------|------|---------|
| 8 | **(推奨)** 残高と入出金の整合 | 前行balance=100000, amount_out=5000 | balance=95000 → 通過 |
| 9 | **(推奨)** 残高大幅差異 | 前行balance=100000, 当行balance=50000, amount_out=5000 | `warn` |

### 8.4 有効/無効分離

| # | テスト内容 | 入力 | 期待結果 |
|---|----------|------|---------|
| 10 | error 行を除外 | 5行中1行がerror | 有効リスト=4行, エラーリスト=1件 |
| 11 | warn 行は残る | 5行中1行がwarn | 有効リスト=5行, エラーリスト(warn)=1件 |
| 12 | 全行正常 | error/warn なし | 有効リスト=全件, エラーリスト=0件 |

---

## 9. ledger_generator.py — 仕訳候補生成

### 9.1 入金/出金→借方貸方変換

| # | テスト内容 | 入力 | 期待結果 |
|---|----------|------|---------|
| 1 | 銀行入金 | `source_type="bank", amount_in=50000` | 借方:普通預金 50000 / 貸方:売上高 50000 |
| 2 | 銀行出金 | `source_type="bank", amount_out=10000` | 借方:経費科目 10000 / 貸方:普通預金 10000 |
| 3 | **(推奨)** クレカ出金 | `source_type="creditcard", amount_out=5000` | 借方:経費科目 5000 / 貸方:未払金 5000 |

### 9.2 科目推定（キーワードマッチ）

| # | テスト内容 | 入力 | 期待結果 |
|---|----------|------|---------|
| 4 | 摘要に「振替」 | summary="口座振替" | 該当する仮科目を割り当て |
| 5 | 摘要に「手数料」 | summary="振込手数料" | debit_account="支払手数料" |
| 6 | 推定不能 | 未知の摘要テキスト | デフォルト科目 + `warn` |

### 9.3 Amazon仕訳

| # | テスト内容 | 入力 | 期待結果 |
|---|----------|------|---------|
| 7 | 売掛金/売上高の仕訳 | Amazon商品代金バケット | `debit_account="売掛金", credit_account="売上高"` |
| 8 | 手数料の仕訳 | Amazon手数料バケット | `debit_account="支払手数料"` |
| 9 | 仮受消費税の仕訳 | Amazon税金バケット | `credit_account="仮受消費税"` |

### 9.4 タグ/摘要

| # | テスト内容 | 入力 | 期待結果 |
|---|----------|------|---------|
| 10 | source_type ベースタグ | `source_type="amazon"`| `tags` に `"amazon"` を含む |
| 11 | 取引性質タグ | 手数料取引 | `tags` に `"fee"` を含む |
| 12 | 摘要整形 | 長い summary+details | 適切な長さに丸められる |

### 9.5 異常系

| # | テスト内容 | 入力 | 期待結果 |
|---|----------|------|---------|
| 13 | 金額両方ゼロの行 | `amount_out=0, amount_in=0` | スキップ + `warn` |

---

## 10. exporter.py — 出力

### 10.1 ファイル生成

| # | テスト内容 | 入力 | 期待結果 |
|---|----------|------|---------|
| 1 | 出力ディレクトリ作成 | 正常な出力パス | `outputs/YYYYMMDD_HHMMSS/` が作成される |
| 2 | normalized_transactions.csv 生成 | 有効な NormalizedTransaction リスト | ファイルが存在し、行数が一致 |
| 3 | ledger_draft.csv 生成 | 有効な LedgerDraft リスト | ファイルが存在し、行数が一致 |
| 4 | error_report.csv 生成（エラーあり） | ValidationError リスト | ファイルが存在し、内容が一致 |
| 5 | error_report.csv 生成（エラーなし） | 空リスト | ヘッダのみのCSVが生成される |
| 6 | smart_import.csv 生成 | 有効な LedgerDraft リスト | スマート取引取込フォーマットに適合 |
| 7 | run_log.txt 生成 | ProcessingSummary | 実行ログが所定フォーマットで出力 |

### 9.2 異常系

| # | テスト内容 | 入力 | 期待結果 |
|---|----------|------|---------|
| 8 | 出力先に書き込み権限なし | 読取専用ディレクトリ | `ExportError` / `IOError` |
| 9 | **(推奨)** ディスク空き不足 | — | `ExportError` |

### 9.3 推奨

| # | テスト内容 | 入力 | 期待結果 |
|---|----------|------|---------|
| 10 | **(推奨)** smart_import.csv のエンコーディング確認 | — | やよい仕様のエンコーディングと一致 |
| 11 | **(推奨)** smart_import.csv の列順確認 | — | やよい仕様の列順と一致 |

---

## 10. pipeline.py — 統合テスト

### 10.1 単一ファイル

| # | テスト内容 | 入力 | 期待結果 |
|---|----------|------|---------|
| 1 | 銀行明細テンプレA E2E | UFJ形式サンプルCSV | 4ファイル生成、サマリの件数/金額が期待値と一致 |
| 2 | 銀行明細テンプレB E2E | 愛知銀行形式サンプルCSV | 4ファイル生成、サマリの件数/金額が期待値と一致 |
| 3 | Amazon決済レポート E2E | Amazonサンプル TSV | バケット集計が正しい、4ファイル生成 |

### 10.2 複数ファイル結合

| # | テスト内容 | 入力 | 期待結果 |
|---|----------|------|---------|
| 4 | 同種複数ファイル | 銀行明細×2 | 全件結合、row_id 衝突なし |
| 5 | 異種ソース混合 | 銀行明細 + Amazon | ソース別サマリが分離して集計 |
| 6 | **(推奨)** 大量データ | 1000行超のファイル | 性能劣化なく完了 |

### 10.3 エラーハンドリング

| # | テスト内容 | 入力 | 期待結果 |
|---|----------|------|---------|
| 7 | 1ファイル判定失敗 + 1ファイル正常 | 不明形式 + 有効銀行CSV | 正常ファイルは処理完了、error_report に判定失敗を記録 |
| 8 | 全ファイル判定失敗 | 不明形式ファイルのみ | `total_count=0`, `error_count>0` |
| 9 | パース中にエラー行あり | 一部行が日付不正 | エラー行を除外して残りは仕訳生成 |

### 10.4 ProcessingSummary

| # | テスト内容 | 入力 | 期待結果 |
|---|----------|------|---------|
| 10 | 期間 (period_start / period_end) | サンプルデータ | 最小日付〜最大日付 |
| 11 | 入出金合計 | サンプルデータ | total_in / total_out が集計値と一致 |
| 12 | source_files 件数 | 2ファイル入力 | `len(source_files) == 2` |
| 13 | ファイルごとの error/warn 件数 | エラー含むファイル | 各 SourceFileInfo の件数が正しい |

---

## 11. 回帰テスト（固定サンプル）

| # | テスト内容 | 方法 | 期待結果 |
|---|----------|------|---------|
| 1 | 銀行テンプレA 固定出力 | fixtures にUFJサンプル（数行）を保存 | normalized / ledger / smart_import の差分が既存スナップショットと一致 |
| 2 | 銀行テンプレB 固定出力 | fixtures に愛知銀行サンプル（数行）を保存 | 同上 |
| 3 | Amazon 固定出力 | fixtures にAmazonサンプル（1精算分）を保存 | 同上 |
| 4 | smart_import.csv 列構成の不変チェック | — | 列名リストがスナップショットと一致 |

---

## 12. 将来対応テスト（未確定事項が確定したら追加）

| # | テスト内容 | 待ち要件 |
|---|----------|---------|
| 1 | **(推奨)** ヘッダ表記揺れ許容（別名辞書） | 別名辞書ロジックの確定 |
| 2 | **(推奨)** balance 整合チェック（警告） | 残高照合ロジックの確定 |
| 3 | **(推奨)** 科目推定キーワードルールの網羅テスト | キーワードリストの確定 |
| 4 | **(推奨)** スマート取引取込CSV列仕様の適合テスト | やよい側の仕様確定 |
| 5 | **(推奨)** クレジットカード明細のパース・正規化 | cc_format_*.yml の定義確定 |
| 6 | **(推奨)** 新カテゴリ追加時の registry 登録 + pipeline 通過 | 対象カテゴリの確定 |
| 7 | **(推奨)** Amazon列名変更時の unmapped 自動検出 | 新しい列名パターンの入手 |
| 8 | **(推奨)** 仕訳日記帳CSV出力（和暦変換・cp932） | 出力仕様の確定 |
