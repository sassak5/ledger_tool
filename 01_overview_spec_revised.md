# 帳簿自動作成ツール 仕様書
更新日: 2026-02-21

---

## 1. 目的 / 成功条件
- 各種明細データ（銀行・Amazon売上・クレジットカード等）を取り込み、帳簿（仕訳日記帳）を自動作成できること
- GUIのボタン操作で「取り込み → 正規化 → ルール適用 → 仕訳候補生成 → 出力」まで完結すること
- 取り込み不可・変換不可はエラーとして一覧化し、原因が追えること
- 弥生へはスマート取引取込にCSVでアップロードできるように整形する(仕訳日記帳に変更予定)

    ### 1.1 前提
    - 弥生へはスマート取引取込にCSVでアップロードできるように整形する(仕訳日記帳に変更予定)
    - 本仕様は「帳簿（仕訳候補）作成まで」のツール内フローを確定させる
    - スマート取引取込ベースでGUIにてデータ確認できるようにする(仕訳日記帳に変更予定)
    - パイプラインの後段（正規化以降）はソース種別を意識しない設計とする（04_module_design.md 参照）

---

## 2. GUIの操作内容（MVP）
- [ファイルを選択]（複数ファイル可。ソース種別はファイル内容から自動判定する）
- [変換（帳簿作成）]
- [出力]
- 表示領域（最低限）
  - 実行ログ（処理段階・件数）
  - サマリ（期間、入金合計、出金合計、件数。ソース別に表示）
  - エラー/警告一覧（行番号、理由、参照ID）
  - 仕訳確認テーブル（スマート取引取込ベース）(仕訳日記帳に変更予定)

---

## 3. 取り込むデータ（MVP）

    ### 3.1 銀行明細（テンプレ判定）
    - ヘッダ（列名）一致によるフォーマット判定で取り込む　#TODO: エラーの仕様固め
    - 初期対応テンプレ（スクショ列名に基づき.ymlの仕様通り）

    #### 3.1.1 フォーマット判定
    - `detector.py` が先頭行（ヘッダ行）の列名集合と `rules/` 配下のYAMLの `header_aliases` を突合する
    - まずは「完全一致/包含一致」を優先
    - ヘッダ名の表記揺れ（全角半角、（円）の有無など）は `header_aliases` に別名を列挙する方式で吸収する
    - 判定結果は `DetectionResult(format_id, source_kind, encoding, config_path)` として返す

    #### 3.1.2 テンプレA（例：三菱UFJ系）
    **期待ヘッダ（列名）**
    - 日付 / 摘要 / 摘要内容 / 支払い金額 / 預かり金額 / 差引残高 / メモ / 未資金化区分 / 入払区分

    **意味（ツール内での扱い）**
    - 支払い金額: 出金（amount_out）
    - 預かり金額: 入金（amount_in）
    - 摘要/摘要内容: summary/details
    - 差引残高: balance（取得できる場合のみ）
    - 未資金化区分/入払区分: extra に保持（判定・学習に使える）

    #### 3.1.3 テンプレB（例：愛知銀行系）
    **期待ヘッダ（列名）**
    - 年月日 / お支払金額 / お預り金額（円） / 内容 / 差引残高 / メモ

    **意味（ツール内での扱い）**
    - お支払金額: 出金（amount_out）
    - お預り金額（円）: 入金（amount_in）
    - 内容: summary（detailsは当面同値でよい）

    ### 3.2 Amazon売上レポート # TODO: 使い勝手を検証する
    - まずは代表1種類に固定（CSV/TSV）
    -（将来）売上・手数料・返金・広告などの分解、および銀行入金との突合を拡張する
    - レポートの列名・amount_description の変更に耐えるため、未知の行は unmapped バケットに集めて警告する

    ### 3.3 クレジットカード明細 [将来]
    - YAML駆動のフォーマット定義で対応
    - 新カテゴリ追加時も pipeline / normalizer 以降のコードは変更不要な疎結合設計

---

## 4. 正規化（内部共通フォーマット）
入力（銀行/Amazon/クレカ/将来の新カテゴリ）の差を吸収し、帳簿作成ロジックはこの形式のみを見る。

    ### 4.0 方針（MVPの位置づけ）
    - 列名マッピング（入力列→正規フィールド）は主にルールYAMLの `header_aliases` で吸収しつつ、正規化ロジック自体は Python コード（`normalizer`）側に実装する
    - 日付/金額の型変換や入出金方向の統一は `normalizer` がハードコードで行う（銀行/Amazon専用ロジック）
    - YAML は ヘッダ別名や Amazon バケット定義、仕訳科目マッピング（ledger_mapping.yml）などの設定に限定して利用する
    - YAMLスキーマ検証や「YAMLフル駆動の正規化DSL」は現時点では未実装とし、将来の拡張案として扱う

    ### 4.1 正規化レコード: NormalizedTransaction
    **フィールド詳細は 05_models_detail.md を参照。**

    - source_type: `"bank"` | `"amazon"` | `"creditcard"` | （将来の新カテゴリ）
    - source_name: 例）`"mufj"` / `"aichi"` / `"amazon_settlement"` / `"rakuten_card"`
    - row_id: 元データ行のユニーク識別子（`"{source_name}_{行番号}"` 形式）
    - date: 取引日（YYYY-MM-DD）
    - summary: 摘要（短い説明）
    - details: 摘要内容/内容（長い説明）
    - amount_out: 支払金額（出金、正の整数）
    - amount_in: 預り金額（入金、正の整数）
    - balance: 差引残高（取得できる場合のみ）
    - memo: メモ（任意）
    - extra: テンプレ固有情報を保持する辞書（未資金化区分、入払区分、支払区分、settlement_id など）
    - raw: 元行データ（デバッグ用）

    ### 4.2 パース中間データ: RawRecord
    各パーサが共通インターフェース (`base_parser`) 経由で返す、正規化前の中間データ。
    **フィールド詳細は 05_models_detail.md を参照。**

    ### 4.3 テンプレ別マッピング（列→正規フィールド）
    - 列名マッピングおよび正規化（型変換/正規表現/補正）は `rules/bank_format_*.yml` 等に定義する
    - GUI からは YAML の **mapping（入力列→正規フィールド）** を編集し、同じ正規フィールドに紐づく正規化ルールは自動適用される
    - 具体例は `03_models_and_specs.md` の YAML 例を参照

---

## 5. 検証（取り込み時チェック）

    ### 5.1 ヘッダ判定チェック
    - 期待ヘッダ集合に一致/包含しているか
    - ヘッダ行が複数行（タイトル行→ヘッダ行）になっていないか
    - 類似列名（例：全角半角、（円）の有無）は将来的に許容（MVPは一致優先）

    ### 5.2 行データチェック（銀行）
    - date がパースできる（例：YYYY/M/D、YYYY/MM/DD）
    - amount_out と amount_in は「どちらか片方のみが入る」こと
        - 両方入る → warn、両方空欄 → warn
    - 数値はカンマ除去で整数化できること（例：43,472）
    - 空行/合計行/見出し行の混入はスキップできること
    - balance がある場合（任意）
        - 前行との差分と入出金が大きく矛盾しないかを警告として出せると良い

    ### 5.3 Amazon固有チェック
    - `total-amount` (ヘッダ行) と各バケット合算値の突合。差異があれば warn
    - unmapped バケットに集まった行の件数・合計金額を warn として報告

    ### 5.4 変換結果サマリ（画面表示）
    - 取り込み件数（ソース別）
    - 期間（最小日付〜最大日付）
    - 入金合計 / 出金合計
    - エラー件数 / 警告件数（例：日付欠損、金額両側入力、unmapped行など）

---

## 6. 帳簿（仕訳候補）生成

    ### 6.1 仕訳候補: LedgerDraft（中間成果物）
    最終的にスマート取引取込CSVに変換される。
    **フィールド詳細は 03_models_detail.md を参照。**

    - date
    - debit_account（借方科目）
    - debit_amount（借方金額）
    - credit_account（貸方科目）
    - credit_amount（貸方金額）
    - description（摘要）
    - partner（取引先/相手先：任意）
    - source_row_id（元データ参照）
    - tags（任意：amazon/fee/refund/creditcard 等）

    ### 6.2 ルール適用（MVPの想定）
    - 入金/出金の判定
        - amount_in > 0 → 入金系
        - amount_out > 0 → 出金系
    - 摘要整形
        - summary + details を連結（冗長なら丸め）
    - 科目推定（暫定）
        - キーワード（例：振替/口座振替/カード）で仮科目を割り当てる
        - 最初は「手動で上書きできる」前提でもOK
        - source_type ごとのデフォルト科目パターン（銀行:普通預金、クレカ:未払金）
    - 学習（将来）
        - ユーザー修正をルールに反映（摘要パターン→科目）する

---

## 7. 出力仕様（MVP）
- normalized_transactions.csv（正規化済みの共通フォーマット）
- ledger_draft.csv（仕訳候補）
- error_report.csv（エラー一覧）
- **スマート取引取込CSV**（やよい取り込み用メイン出力）

### 7.1 出力フォルダ方針（例）
  - outputs/YYYYMMDD_HHMMSS/
    - normalized_transactions.csv
    - ledger_draft.csv
    - error_report.csv
    - smart_import.csv（スマート取引取込用）
    - run_log.txt（任意）

---

## 8. スコープ外（MVPでは対応しない）
- 仕訳の完全自動確定（必ずしも人の確認を不要にはしない）
- 複数Amazonアカウント・複数銀行の完全同時対応（まずはテンプレ追加で段階対応）
- インボイス制度・消費税の厳密処理（MVPは税込固定など簡略化）
- 帳簿以外の会計処理（決算処理等）

---

## 9. 設計方針（04_module_design.md より）
- **ソース非依存**: 正規化以降はソース種別を意識しない。NormalizedTransaction に集約
- **設定駆動**: フォーマット定義・集計ルール・科目マッピングはすべて YAML
- **パーサ疎結合**: 共通インターフェース (base_parser) + レジストリ (registry) で新カテゴリをコード最小変更で追加可能
- **Amazon揺れ耐性**: unmapped バケット + total-amount 突合で列名変更を自動検出

---

## 10. 未決事項 / 確認ポイント（随時追記）
- 銀行明細の入力形式は CSV / Excel（.xlsx）どちらを主とするか ← csv
- 同一銀行でも列名が揺れる場合の許容（別名辞書/手動マッピングUI） ← ymlあるいは正規表現
- Amazon売上の粒度（明細仕訳か、月次まとめ仕訳か） ← 振込タイミングでの明細仕分け。なので銀行口座入金の詳細が振替伝票としてここに来る
- スマート取引取込の列仕様の確定（やよい側の仕様確認）
- 取引先名や補助科目をどこまで自動化するか
- 税区分・インボイス関連の扱い（MVPは簡略化、後で確定）

---
